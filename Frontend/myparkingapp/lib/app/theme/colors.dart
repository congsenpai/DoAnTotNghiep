import 'package:flutter/material.dart';


class AppColors {
  // Màu chính (đại diện cho thương hiệu, icon chính)
  static const Color primary = Color(0xFF007AFF); // Xanh dương
  static const Color primaryDark = Color(0xFF0056B3); // Xanh dương đậm

  // Màu phụ (dùng cho các điểm nhấn, nút quan trọng)
  static const Color secondary = Color(0xFFFFCC00); // Vàng nổi bật
  static const Color secondaryDark = Color(0xFFC79A00); // Vàng đậm hơn

  // Màu nền (giao diện sáng và tối)
  static const Color background = Color(0xFFF8F9FA); // Xám nhạt, gần trắng
  static const Color backgroundDark = Color(0xFF1E1E1E); // Màu nền tối (Dark mode)

  // Màu trạng thái
  static const Color success = Color(0xFF34C759); // Xanh lá cây - Thành công
  static const Color warning = Color(0xFFFFA500); // Cam - Cảnh báo
  static const Color error = Color(0xFFFF3B30); // Đỏ - Lỗi

  // Màu văn bản
  static const Color textPrimary = Color(0xFF212121); // Đen xám
  static const Color textSecondary = Color(0xFF757575); // Xám trung bình

  // Màu đường viền & phân cách
  static const Color border = Color(0xFFBDBDBD); // Xám nhạt
}
